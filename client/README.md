# CRM Client

Development:

npm install
npm run dev

Environment:
- VITE_API_BASE - set to http://localhost:4000 by default

Notes:
- Login using the seeded users in `server/data/db.json` (admin@example.com/adminpass, manager@example.com/managerpass, user@example.com/userpass)
